// Optional: Global setup for Jest tests
// For example, you might want to connect to a test database here
// or mock global modules

// Example:
// jest.setTimeout(30000); // Set default timeout for all tests

// If you're using environment variables for tests (e.g., a test database URI)
// require('dotenv').config({ path: '.env.test' }); // Make sure to create a .env.test file

// beforeAll(async () => {
//   // Connect to test database
// });

// afterAll(async () => {
//   // Disconnect from test database
// });
